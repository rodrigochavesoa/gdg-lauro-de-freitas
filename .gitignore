node_modules
.cursor/
AGENTS.md
dist
.pnpm-store
.env
.env.*
!.env.example
*.local
coverage

# Documentação operacional — somente local (nunca commitar)
# Cobre docs-local/clickup.env (token ClickUp) e demais runbooks
docs-local/

# Legado — conteúdo operacional vive em docs-local/ (não recriar no Git)
docs/
