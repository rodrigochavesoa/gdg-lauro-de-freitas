# Curador de teste — S4-01 (gitignored)

Copie para `docs-local/curator-test-user.md` e preencha com a conta de homologação.

- E-mail: curator-homolog@example.invalid
- Senha: (definir no Supabase Auth; não colar aqui se o arquivo for compartilhado)
- profiles.role: curator
- Criado/atualizado: (data)

Nunca commitar `docs-local/`.
